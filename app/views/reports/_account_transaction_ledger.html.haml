=partial :form
- @keys = [:date, :created_at,:received_for, :type, :amount, :client, :branch, :center, :product, :fee_name]
%table.report
  %tr.header
    - @keys.each do |k|
      %th
        = k
  - @data.each do |d|
    %tr
    - @keys.each do |k|
      %td
        = d[k]
